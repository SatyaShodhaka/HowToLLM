

# Define model configurations
model:
  num_layers: 12
  d_model: 768
  nhead: 12
  dim_feedforward: 3072
  dropout: 0.1
  max_seq_length: 2048

# Training configurations
training:
  batch_size: 16
  num_epochs: 10
  learning_rate: 5e-5
  weight_decay: 0.01
  warmup_steps: 1000
